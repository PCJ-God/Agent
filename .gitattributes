# 行尾策略
#
# 背景：这个仓库在 Windows 上开发（core.autocrlf 会把检出转成 CRLF），
# 但下面这些文件是要复制到 Linux 上执行的。带 CRLF 的 shell 脚本会直接报
#   /usr/bin/env: 'bash\r': No such file or directory
# systemd 单元文件与 nginx 配置带 CRLF 也可能出现难查的问题。
#
# 所以对它们强制 LF：仓库里存 LF，检出到任何平台也还是 LF。

*.sh           text eol=lf
deploy/*       text eol=lf
*.service      text eol=lf

# 其余文本文件按平台默认处理（Windows 检出 CRLF，Linux 检出 LF）
*              text=auto
